```java
class Element {
    int val, row, col;
    Element(int val, int row, int col) {
        this.val = val;
        this.row = row;
        this.col = col;
    }
}

class Solution {
    public ArrayList<Integer> mergeArrays(int[][] mat) {
        ArrayList<Integer> result = new ArrayList<>();
        int k = mat.length;
        
        // Min-Heap based on the value of the elements
        PriorityQueue<Element> minHeap = new PriorityQueue<>((a, b) -> a.val - b.val);
        
        // 1. Put the first element of each of the k arrays into the heap
        for (int i = 0; i < k; i++) {
            if (mat[i].length > 0) {
                minHeap.add(new Element(mat[i][0], i, 0));
            }
        }
        
        // 2. Process the heap
        while (!minHeap.isEmpty()) {
            Element curr = minHeap.poll();
            result.add(curr.val);
            
            // 3. If there's a next element in the same row, add it to the heap
            if (curr.col + 1 < mat[curr.row].length) {
                minHeap.add(new Element(mat[curr.row][curr.col + 1], curr.row, curr.col + 1));
            }
        }
        
        return result;
    }
}
```
