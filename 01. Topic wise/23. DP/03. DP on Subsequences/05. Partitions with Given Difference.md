```java
int countPartitions(int[] arr, int diff) {
    int totalSum = 0;
    for (int x : arr) totalSum += x;

    // Condition check: (totalSum + diff) must be even and totalSum >= diff
    if ((totalSum + diff) % 2 != 0 || totalSum < diff) {
        return 0;
    }

    int target = (totalSum + diff) / 2;
    return perfectSum(arr, target);
}
```
