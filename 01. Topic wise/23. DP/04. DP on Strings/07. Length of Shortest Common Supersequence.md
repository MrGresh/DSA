```java
int minSuperSeq(String str1, String str2) {
    int n = str1.length();
    int m = str2.length();
    
    // Use your existing LCS logic
    int lcsLength = longestCommonSubsequence(str1, str2);
    
    // Apply the formula
    return n + m - lcsLength;
}
```
