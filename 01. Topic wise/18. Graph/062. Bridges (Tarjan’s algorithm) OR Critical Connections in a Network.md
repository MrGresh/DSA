```java
class Solution {
    private int timer = 0;
    List<List<Integer>> bridges;
    int[] disc;
    int[] low;
    boolean[] visited;
    List<List<Integer>> adj;
    public List<List<Integer>> criticalConnections(int n, List<List<Integer>> connections) {
        adj = new ArrayList<>();
        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());
        for (List<Integer> edge : connections) {
            int u = edge.get(0);
            int v = edge.get(1);
            adj.get(u).add(v);
            adj.get(v).add(u);
        }

        disc = new int[n]; // Discovery time
        low = new int[n];  // Lowest discovery time reachable
        visited = new boolean[n];
        bridges = new ArrayList<>();

        // 2. Start DFS
        dfs(0, -1);

        return bridges;
    }

    private void dfs(int u, int p) {
        visited[u] = true;
        disc[u] = low[u] = timer++;

        for (int v : adj.get(u)) {
            if (v == p) continue; // skip parent

            if (visited[v]) {
                low[u] = Math.min(low[u], disc[v]);
            } else {
                dfs(v, u);
                low[u] = Math.min(low[u], low[v]);

                // Bridge check
                if (low[v] > disc[u]) bridges.add(Arrays.asList(u, v));
            }
        }
    }
}
```
// Without visited array
```java
class Solution {
    private int timer = 0;
    List<List<Integer>> bridges;
    int[] disc;
    int[] low;
    List<List<Integer>> adj;
    public List<List<Integer>> criticalConnections(int n, List<List<Integer>> connections) {
        adj = new ArrayList<>();
        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());
        for (List<Integer> edge : connections) {
            int u = edge.get(0);
            int v = edge.get(1);
            adj.get(u).add(v);
            adj.get(v).add(u);
        }

        disc = new int[n]; // Discovery time
        low = new int[n];  // Lowest discovery time reachable
        bridges = new ArrayList<>();

        // 2. Start DFS
        dfs(0, -1);

        return bridges;
    }

    private void dfs(int u, int p) {
        disc[u] = low[u] = timer++;

        for (int v : adj.get(u)) {
            if (v == p) continue; // skip parent

            if (disc[v] > 0) { // already visited
                low[u] = Math.min(low[u], disc[v]);
            } else {
                dfs(v, u);
                low[u] = Math.min(low[u], low[v]);

                // Bridge check
                if (low[v] > disc[u]) bridges.add(Arrays.asList(u, v));
            }
        }
    }
}
```