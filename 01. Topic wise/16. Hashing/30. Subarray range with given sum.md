```java
class Solution {
    static int subArraySum(int arr[], int tar) {
        Map<Integer, Integer> map = new HashMap<>();
        int count = 0;
        map.put(0, 1);
        for (int i = 0; i < arr.length; i++) {
            if(i>0) arr[i] += arr[i-1];
            if (map.containsKey(arr[i] - tar)) {
                count += map.get(arr[i] - tar);
            }
            map.put(arr[i], map.getOrDefault(arr[i], 0) + 1);
        }
        return count;
    }
}
```
```java
class Solution {
    static int subArraySum(int arr[], int tar) {
        Map<Integer, Integer> map = new HashMap<>();
        int count = 0;
        for (int i = 0; i < arr.length; i++) {
            if(i>0) arr[i] += arr[i-1];
            if(arr[i] - tar == 0) count += 1;
            else if (map.containsKey(arr[i] - tar)) {
                count += map.get(arr[i] - tar);
            }
            map.put(arr[i], map.getOrDefault(arr[i], 0) + 1);
        }
        return count;
    }
}
```
