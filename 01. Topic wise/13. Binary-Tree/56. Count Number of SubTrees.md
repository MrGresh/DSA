```java
class Solution {
    int count;
    int countSubtrees(Node root, int x) {
        count = 0;
        rec(root, x);
        return count;
    }
    int rec(Node root, int x) {
        if(root==null) return 0;
        int sum = root.data;
        if(root.left!=null) sum += rec(root.left, x);
        if(root.right!=null) sum += rec(root.right, x);
        if(sum==x) count++;
        return sum;
    }
}
```
