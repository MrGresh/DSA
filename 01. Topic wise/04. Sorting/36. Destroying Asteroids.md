```java
class Solution {
    public boolean asteroidsDestroyed(int mass, int[] asteroids) {
        long currMass = mass;
        Arrays.sort(asteroids);
        for(int x: asteroids) {
            if(x>currMass) return false;
            currMass += x;
        }
        return true;
    }
}
```
