```java
class Solution {
    public int visibleBuildings(int arr[]) {
        int ans = 0, currMax = Integer.MIN_VALUE;
        for(int x: arr) {
            if(x>=currMax) {
                currMax = x;
                ans++;
            }
        }
        return ans;
    }
}
```
